{
  "name": "example-workspace-config",
  "imports": ["./variables.json", "./variableGroups.json"],
  "plugins": ["shc-plugin-example"],
  "hooks": {
    "pre-request": ["example.auth", "base.logContext"],
    "post-request": ["base.logContext"]
  },
  "endpoints": {
    "getSomething": {
      "method": "GET",
      "endpoint": "{{baseUrl}}/get"
    }
  }
}
