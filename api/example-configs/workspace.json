{
  "name": "example-workspace-config",
  "imports": ["./variables.json"],
  "plugins": ["shc-plugin-example"],
  "hooks": {
    "pre-request": ["example.auth"]
  },
  "endpoints": {
    "getSomething": {
      "method": "GET",
      "endpoint": "{{baseUrl}}/something"
    }
  }
}
