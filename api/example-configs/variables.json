{
  "imports": ["./nestedImport.json"],
  "plugins": ["shc-plugin-example"],
  "pluginConfig": {
    "example": {
      "baseUrl": "{{environment.api-gw-url}}"
    }
  },
  "variables": {
    "my-second-url": "{{example.httpBinUrl}}"
  }
}
